{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "deploy/Dockerfile.collector"
  },
  "deploy": {
    "startCommand": "python -m ibkr.dynamic_collector --port 4004",
    "restartPolicyType": "ALWAYS"
  }
}
